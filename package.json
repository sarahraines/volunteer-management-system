{
  "name": "client-build",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "engines": {
    "yarn": "1.22.5"
  },
  "scripts": {
    "start": "echo start",
    "build": "cd client && yarn build"
  }
}
